# Как добавить видео или GIF на фон

## 📁 Структура папок

Создайте папки для медиафайлов:
- `public/videos/` - для видео файлов
- `public/images/` - для GIF файлов

## 🎥 Для видео (MP4, WebM)

1. Поместите видео файл в папку `public/videos/`
2. Откройте `src/app/page.tsx`
3. Найдите закомментированный код с `VideoBackground`
4. Раскомментируйте и укажите путь к вашему файлу:

```tsx
<VideoBackground 
  src="/videos/background.mp4" 
  type="video"
  overlay={true}
  overlayOpacity={0.6}
/>
```

## 🖼️ Для GIF

1. Поместите GIF файл в папку `public/images/`
2. Откройте `src/app/page.tsx`
3. Раскомментируйте код для GIF:

```tsx
<VideoBackground 
  src="/images/background.gif" 
  type="gif"
  overlay={true}
  overlayOpacity={0.5}
/>
```

## ⚙️ Настройки

- `src` - путь к файлу (начинается с `/`)
- `type` - "video" или "gif"
- `overlay` - добавить затемнение для читаемости текста (true/false)
- `overlayOpacity` - прозрачность затемнения (0-1, где 0 = прозрачно, 1 = непрозрачно)

## 💡 Рекомендации

- Используйте короткие видео (5-10 секунд) для лучшей производительности
- Оптимизируйте размер файлов (сжимайте видео/GIF)
- Рекомендуемый формат видео: MP4 (H.264)
- Для GIF используйте оптимизированные файлы

## 📝 Пример использования

```tsx
<section className="relative min-h-screen">
  <VideoBackground 
    src="/videos/code-background.mp4" 
    type="video"
    overlay={true}
    overlayOpacity={0.7}
  />
  <div className="relative z-10">
    {/* Ваш контент здесь */}
  </div>
</section>
```

